{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "hugo",
  "buildCommand": "(pip3 install --quiet --disable-pip-version-check --break-system-packages -r requirements.txt || pip3 install --quiet --disable-pip-version-check -r requirements.txt) && python3 scripts/validate.py && npm run build",
  "outputDirectory": "public",
  "build": {
    "env": {
      "HUGO_VERSION": "0.166.0"
    }
  },
  "redirects": [
    {
      "source": "/:path*",
      "has": [
        {
          "type": "host",
          "value": "smaddanki.vercel.app"
        }
      ],
      "destination": "https://smaddanki.com/:path*",
      "permanent": true
    }
  ],
  "installCommand": "npm install"
}
